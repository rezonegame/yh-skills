# Board Game Ralph Loop - 快速开始

## 什么是 Board Game Ralph Loop？

一个让 Claude Code 自主迭代设计桌面游戏的系统。你定义好设计目标，然后去睡觉，醒来时游戏设计就完成或改进了！

## 核心原理

```
定义 GRD → 自主迭代 → 测试反馈 → 数据分析 → 循环改进 → 达成目标
   (游戏需求文档)    (设计/测试/平衡)  (适应度分数)
```

## 5 分钟快速开始

### 1. 创建游戏项目

```bash
mkdir my-game-design
cd my-game-design

# 复制 Ralph Loop 模板文件
cp -r ~/.claude/skills/boardgame-ralph-loop/* .
```

### 2. 编辑 grd.json

```json
{
  "gameName": "我的游戏",
  "version": "v0.1.0",
  "gameOverview": {
    "theme": "资源管理游戏",
    "playerCount": {"min": 2, "max": 4}
  },
  "fitnessGoals": {
    "clarity": {"current": 0, "target": 8.0},
    "strategy": {"current": 0, "target": 7.5},
    "interaction": {"current": 0, "target": 7.0},
    "balance": {"current": 0, "target": 7.5},
    "fit": {"current": 0, "target": 8.0}
  }
}
```

### 3. 添加设计任务

```json
"design_tasks": [
  {
    "id": "DT-001",
    "title": "设计核心机制",
    "acceptanceCriteria": [
      "定义回合结构",
      "描述资源系统",
      "文档完整"
    ],
    "priority": 1,
    "passes": false
  }
]
```

### 4. 启动自主循环

```bash
# 设置脚本可执行
chmod +x scripts/ralph-loop.sh

# 运行（最多30次迭代）
./scripts/ralph-loop.sh 30

# 去睡觉吧！
```

### 5. 醒来查看结果

```bash
# 查看任务完成情况
cat grd.json | jq '.design_tasks[] | {id, passes}'

# 查看学习日志
cat progress.txt

# 查看 git 提交历史
git log --oneline
```

## 交互式使用

如果你想在每次迭代时查看和确认：

```
# 在 Claude Code 中
"读取 grd.json，选择下一个未完成任务并执行"
```

## 任务编写技巧

### ✅ 好的任务

```json
{
  "title": "设计卡牌系统",
  "acceptanceCriteria": [
    "定义5种卡牌类型",
    "设计成本曲线公式",
    "创建10张示例卡牌",
    "验证平衡性（成本-效果比一致）"
  ]
}
```

### ❌ 不好的任务

```json
{
  "title": "让游戏好玩",
  "acceptanceCriteria": ["好玩就行"]
}
```

## 常用命令

```bash
# 查看进度
cat grd.json | jq '.metadata'

# 查看适应度分数
cat grd.json | jq '.fitnessGoals'

# 查看下一个任务
cat grd.json | jq '[.design_tasks[], .playtest_tasks[], .balance_tasks[] | select(.passes == false)] | sort_by(.priority) | .[0]'

# 查看学习模式
cat progress.txt | grep -A 20 "## Design Patterns"
```

## 适应度评分参考

| 分数 | Clarity | Strategy | Interaction | Balance | Fit |
|-----|---------|----------|-------------|---------|-----|
| 10 | 5分钟理解 | 多策略 | 频繁互动 | 完美平衡 | 完美契合 |
| 8 | 需简要说明 | 多策略 | 定期互动 | 基本平衡 | 贴合主题 |
| 5 | 需详细解释 | 部分策略 | 偶尔互动 | 轻微不平衡 | 部分脱节 |
| 3 | 规则有歧义 | 有限选择 | 很少互动 | 主导策略 | 冲突明显 |
| 1 | 无法理解 | 随机/单一 | 无互动 | 严重失衡 | 无关主题 |

## 停止条件

Ralph Loop 在以下情况自动停止：

1. ✅ 所有任务 passes = true
2. ✅ 所有适应度分数达到目标

输出：`<promise>DESIGN_COMPLETE</promise>`

## 故障排除

### Q: 循环一直在运行，不停止？

A: 检查任务是否可验证：
```bash
cat grd.json | jq '.design_tasks[].acceptanceCriteria'
```

### Q: 没有创建 git commit？

A: 确保在 git 仓库中：
```bash
git init
git add .
git commit -m "Initial: Start Ralph Loop"
```

### Q: 适应度分数不更新？

A: 确保有 playtest 任务，测试后才会更新分数。

## 下一步

- 阅读 [SKILL.md](../SKILL.md) 了解完整功能
- 阅读 [five-component-filter.md](./five-component-filter.md) 了解评分系统
- 阅读 [metrics_schema.md](./metrics_schema.md) 了解数据结构

---

**祝你的游戏设计愉快！** 🎲
