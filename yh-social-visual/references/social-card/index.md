# Social Card Reference Index

- Start with `content-planning.md` to split the source into a card sequence.
- Read `platform-specs.md` before choosing ratio, safe area, and density.
- Use `layout-recipes.md` to select a page structure and `theme-presets.md` for the visual system.
- Read `style-system.md`, `components.md`, and `background-systems.md` when building custom HTML.
- Use `image-overlay.md`, `portrait-fill.md`, `screenshot-treatment.md`, or `map-component.md` only when those asset types appear.
- Follow `production-workflow.md` for rendering and `qa-checklist.md` before delivery.
- Use `title-shortener.md` when a headline does not fit safely.
- Use `category-cookbook.md` for domain-specific sequencing examples.
