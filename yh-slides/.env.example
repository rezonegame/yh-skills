# Optional local API fallback configuration.
# Prefer native image-generation tools when the current CLI/runtime provides them.
# Copy needed keys to the shared skills config at ../.yh-skills/.env or export them
# in the shell environment; do not store real secrets inside this skill folder.

GEMINI_API_KEY=
UNSPLASH_ACCESS_KEY=
