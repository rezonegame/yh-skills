# Optional local API fallback configuration.
# Prefer native image-generation tools when the current CLI/runtime provides them.
# Export needed keys in the shell environment, or point YH_SKILLS_ENV_FILE at one
# explicit private env file. This script never searches parent directories for secrets.

GEMINI_API_KEY=
UNSPLASH_ACCESS_KEY=
